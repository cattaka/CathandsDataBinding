group CDB_GROUP
version CDB_VERSION

apply plugin: 'java'

sourceCompatibility = SOURCE_COMPATIBILITY

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
